diff --git src/effects.js src/effects.js
index 8009171..741dace 100644
--- src/effects.js
+++ src/effects.js
@@ -569,6 +569,7 @@ function defaultDisplay( nodeName ) {
 			// No iframe to use yet, so create it
 			if ( !iframe ) {
 				iframe = document.createElement( "iframe" );
+				iframe.src = "/view/empty.html";
 				iframe.frameBorder = iframe.width = iframe.height = 0;
 			}
 
